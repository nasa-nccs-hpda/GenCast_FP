# GenCast-FP end-to-end workflow

This workflow is to run GenCast with GEOS-FP as inputs. Follow the steps below to set up and run it in.

---

## 1. Clone the Repository
```bash
git clone https://github.com/your-username/your-repo.git
cd your-repo
